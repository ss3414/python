# ****************************************************************分割线****************************************************************
# todo tqdm

import time

import tqdm

# # 传入可迭代对象
# for i in tqdm.tqdm([1, 2, 3, 4, 5]):
#     time.sleep(1)

# 三角形进度条
for i in tqdm.tqdm([1, 2, 3, 4, 5]):
    print()  # 有print函数才会形成三角形进度条
    time.sleep(1)
